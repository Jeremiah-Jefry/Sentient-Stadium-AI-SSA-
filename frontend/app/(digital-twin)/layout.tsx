/**
 * Digital Twin layout - minimal layout for the full-screen map view.
 */

export default function DigitalTwinLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
